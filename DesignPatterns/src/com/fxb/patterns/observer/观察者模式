
---------- 观察者模式 ------------
定义：根据被观察对象的状态改变主动发送通知给观察者

1、 带有状态的 具体被观察者角色 和 接受状态改变的观察者角色 通过他们的接口来联系
    这样子两者之间就不需关心 具体的观察者或被观察对象了

2、 a 利用抽象类和接口从具体类中 抽象出抽象的方法
    b 在将实例作为参数传递时，将其转换成抽象类或接口形式 易于替换具体类


注意： 当被观察对象的状态可能会被观察者的行为影响时 需要谨慎观察者之间的关系和调用顺序
       设计时 应当 使被观察对象尽量少知道观察者角色内部的实现